name: demo
on:
  workflow_dispatch:
jobs:
  buildusingmaven:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.1

      - name: build using maven
        run: mvn -B package
          
      - name: list
        run: ls -lh target/
      - name: tesing
        run: mvn test
